# LEARNINGS — 039-koiking

## 2026-08-26 | タイトル鯉を画像のシルエットにする

- タイトルの手描き緋・墨は外し、`Remove_background-1787743120785.png` のアルファ輪郭だけを SVG パスにした。内部の模様は塗らない。飛沫は画像側の粒を残し、幾何の三角飛沫は外した。
- 検証: WebKit iPhone13 でタイトルと `.setsuna-leap` を撮影。実機は未検証。PNG は実行時に読まない。

## 2026-08-26 | プレイ中の鯉をスプライトへ戻す

- 手描きの魚型はフォールバックだけに残し、プレイ中は `images/koi-sprite-sheet.png` を再び読む。
- 検証: スプライト読み込みを復元。実機表示は未検証。

## 2026-08-26 | タイトル鯉を最初のSVGへ戻す

- 楔や直線の魚型は外し、「タイトルの鯉のSVGだけもう少し考えて」のときの一跳ね（白地・緋・墨・ひげ・尾）に戻した。
- 検証: パスを当時のSVGへ復元。実機は未検証。

## 2026-08-26 | iOSの二度押しで跳ねる

- ズーム防止の `touchstart preventDefault` が、二度目の `pointerdown` を潰す。跳ね判定は `touchend`（capture）に移し、間隔 40〜450ms・140px 以内。ボタン上は1回押しのまま。
- 検証: WebKit iPhone13 でプレイ中にキャンバスを二度タップし、跳ねクールダウンを確認。実機は未検証。

## 2026-08-26 | 丸い鯉を魚型に直す

- タイトルは短いカプセル＋丸い緋で団子に見えていた。胴を細長く、吻は直線の楔、尾は切れ込み、緋は頭先を覆わない。
- プレイの丸いスプライトは読まない。上向きの手描き鯉（尖った吻・二又の尾）に切り替える。`shadowBlur` は付けない。
- 検証: 下記スクリーンショット。実機は未検証。

## 2026-08-26 | iOS 75/25とハーネス

- `#game-shell` を 75% ステージ / 25% 操作盤にした。跳ねとミュートは盤上の 56px 以上。キャンバスは `#screen-wrap` に合わせ、`data-logical-width` を付けた。
- ミュートは `localStorage`。BGM・SE・滝音を同時に落とす。`touchmove` 防止と `setPointerCapture` を戻した。
- 検証: `_tools/game-harness.sh 039-koiking` RESULT PASS（2026-08-26T11-03-52）。実機スピーカー・ノッチは未検証。

## 2026-08-26 | BGMを載せる

- Suno の 4分32秒ステレオを `audio/bgm.m4a`（AAC 72kbps / 2.4MB）に落とし、最初のタップで loop 再生。元 MP3（6MB）は公開実体から外す。
- iOS はジェスチャと同じティックで `play()`。タブを隠したら pause、戻ったら再開。滝の AudioWorklet は 0.035 に下げて帯域を空ける。
- 検証: ファイル 200・開始タップ後に paused=false。実機スピーカーは未検証。

## 2026-08-26 | タイトル鯉のシルエットを直す

- 黒い塗りで形を見ると、尾が胴から切れて二本の棒になっていた。胴・背びれ・扇の尾を重ねて一本の魚型にした。
- 検証: WebKit で黒塗りシルエットを画素として確認。実機は未検証。

## 2026-08-26 | タイトルの鯉SVGを描き直す

- 楕円の重ねをやめ、三色（白地・緋・墨）・ひげ・尾びれ・蹴った水冠の一跳ねにした。`feTurbulence` は使わない。
- 検証: WebKit iPhone13 / SE で開始画面を撮影。実機表示は未検証。

## 2026-08-26 | 滝が下へ落ちるように直す

- WebGL の `gl_FragCoord` は下が原点なのに、白波を canvas と同じ向きで動かしていた。`uv.y` を反転し、ノイズも下方向に流す。
- 検証: WebGL の輝度ピークが時間とともに canvas 下方向へ動くことを確認。実機は未検証。

## 2026-08-26 | 竹と紅葉をSVGで描く

- 笹の三葉・節のある竹稈・鋸歯の紅葉（朱／金）を `images/` のSVGにし、起動時にキャンバスへ焼いて層の舞いと岸の風景に使う。`feTurbulence` は使わない。
- 検証: WebKit で竹林層・紅葉層を撮影。実機表示は未検証。

## 2026-08-26 | タイトルを刹那の見開きへ

- 説明文の扁額カードをやめ、滝を舞台にした縦書きタイトル＋跳ねる鯉＋金の一閃にした。開始中はゲームの鯉を描かない。操作は一行の註に落とす。
- 検証: WebKit iPhone13 / SE で開始画面を撮影。実機表示は未検証。

## 2026-08-26 | 滝の流れを5技術で実装

- WebGL2シェーダで落下水流、OffscreenCanvasでノイズ焼き、createImageBitmapでGPU渡し、Float32Arrayで飛沫、AudioWorkletで滝音。失敗時は従来の2Dグラデへ戻す。
- 開始タップ前に初期化。実機のWebGL負荷は未検証。

## 2026-08-26 | タイトル画面を和風扁額に

- 丸角の汎用カードをやめ、漆地に金の入れ子枠・登龍門の額・朱印・青海波で開始画面を組み直した。Google Fonts は切って端末明朝にした（iOSのヒラギノが本体）。
- 検証: WebKit でタイトル画面を撮影。実機表示は未検証。

## 2026-08-26 | 跳ねをダブルクリックでも発火

- プレイ中の2回タップ（40〜340ms、72px以内）と `dblclick` の両方で跳ねる。ズーム防止の `preventDefault` は残す。ボタン上は既存の1回押しを優先。

## 2026-08-26 | 開始固まり＋障害物間隔

- 開始タップを3回計測すると、毎回 72〜97ms の長フレームが1発。原因は `click`+`touchstart` の二重開始、オーバーレイの `backdrop-filter`、丸太SVGの `feTurbulence` 初回ラスタ、`shadowBlur` 付きの初回描画が同じフレームに乗ること。
- 対策: `pointerdown` のみ・開始を次フレームへ送る・丸太をキャンバスへ焼き込み・開始画面の blur 廃止・開始時の障害物を間引く。
- 障害物の縦間隔は開始1.8秒後で 3〜7px。最大12体・再出現が画面直前（-30〜-180px）だった。最小間隔を約 0.48H（下限300px）、初期3体を画面外に段差配置、序盤8mは追加スポーンなし。
- 検証: WebKit iPhone13 相当。開始3回は開始後フレーム 19〜20ms（32ms超え0）。無操作6秒生存は 3/3。縦間隔約319px。タップから動き出すまで 80〜120ms は開始を次フレームへ送った分。実機サーマルは未検証。

## 2026-08-26 | FPS / データ処理の軽量化

- スプライト `images/koi-sprite-sheet.png` が 2304×3072 / 2.9MB で、毎フレーム `shadowBlur` 付き `drawImage`（軌跡は最大10枚）していた。Mac Retina では backing store が 2880×1800 になり、iOS ではデコードと GPU 帯域で詰まる。
- 画像を 576×768（65KB）へ縮小。
- 検証: WebKit スモーク。HTML側の hitch 対策は上のエントリで実施。実機サーマルは未検証。
